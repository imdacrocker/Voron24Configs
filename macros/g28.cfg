[gcode_macro G28]
rename_existing: G28.1
gcode:
    {% set prev = printer["gcode_macro _TOOL_LED_CONF"].state %}
    {% if prev != 'homing' %}
        _SET_TOOL_LED_STATE STATE=homing
    {% endif %}
 
    G28.1 {rawparams}
 
    {% if prev != 'homing' %}
        M400
        _SET_TOOL_LED_STATE STATE={prev}
    {% endif %}